#!/bin/bash
# ===================================================================
# Demo Environment Configuration
# Shared variables to ensure consistency across all scripts
# ===================================================================

# Core Settings
export RANDOM_SUFFIX="dev01"
export ADMIN_USERNAME="rheluser"
export DEFAULT_LOCATION="eastasia"
export RESOURCE_GROUP_NAME="cert-renewal-demo-rg"

# SSH Configuration
export SSH_KEY_NAME="ansible-demo-key"
export SSH_KEY_PATH="$HOME/.ssh/${SSH_KEY_NAME}.pub"
export PRIVATE_KEY_PATH="$HOME/.ssh/${SSH_KEY_NAME}"

# File Paths
export SECRETS_DIR="terraform/secrets"
export ENCODED_FILE="$SECRETS_DIR/windows-admin.b64"
export INSTALLER_PATH="downloads/aap-setup-2.5.tar.gz"

# Network Configuration
export JUMP_HOST_IP_STATIC="10.0.1.10"
export AAP_HOST_IP="10.0.1.12"
export RHEL_WEB_IP="10.0.1.11"
export WIN_WEB_IP="10.0.1.13"
export AD_PKI_IP="10.0.1.14"

# Git Repository
export GIT_REPO_URL="https://github.com/Deim0s13/ansible-cert-renewal-demo.git"

# Logging
export LOG_DIR="logs"
mkdir -p "$LOG_DIR"

# Colors for output (demo-friendly)
export RED='\033[0;31m'
export GREEN='\033[0;32m'
export YELLOW='\033[1;33m'
export BLUE='\033[0;34m'
export NC='\033[0m' # No Color

# Helper functions for consistent logging
log_info() {
    echo -e "${BLUE}ℹ️  $1${NC}"
}

log_success() {
    echo -e "${GREEN}✅ $1${NC}"
}

log_warning() {
    echo -e "${YELLOW}⚠️  $1${NC}"
}

log_error() {
    echo -e "${RED}❌ $1${NC}"
}

log_step() {
    echo -e "\n${BLUE}🚀 Step: $1${NC}"
}
